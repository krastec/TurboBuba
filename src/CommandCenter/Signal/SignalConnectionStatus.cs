using System;
using System.Collections.Generic;
using System.Text;

namespace CommandCenter.Signal
{
    public enum SignalConnectionStatus
    {
        Unknown = 0,
        Connected = 1,
        Disconnected = 2,
        Connecting = 3
    }
}
